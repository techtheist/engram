---
description: Digest this project into Engram — an explicit, one-time ingestion of the current working tree into typed memory nodes.
---

The user wants this project digested into its Engram memory graph.

Invoke the `engram-digest` skill and follow it end to end: preflight the wiring, run the offline `FIXME`/`TODO` scan via the daemon's `POST /digest/scan`, read the project's reasoning surfaces, author nodes per the skill's worked examples under a single `digest-<YYYY-MM-DD>` session id, act on every write verdict, and finish with an audible summary plus a pointer to the pane.

If the `engram` MCP tools are not available in this session, stop and run `/engram:setup` first — never digest into an unwired repo.

$ARGUMENTS names a narrower scope (a directory or subsystem) when present; otherwise digest the whole working tree.
