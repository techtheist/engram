# Dev only: the Vite dev server (port 5173) is a different origin from the
# daemon, so point the API at it explicitly. Production builds use same-origin
# (empty base) because the daemon serves the built pane itself.
VITE_ENGRAM_API=http://localhost:8787
