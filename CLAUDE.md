# Engram — context for Claude Code

**What this is:** a graph-based, durable, inspectable long-term project memory for AI coding assistants (Claude Code first). Local-first, user-owned, graph-first UI. See **`PLAN.md`** — it is the source of truth; read it before doing anything.

**Positioning (PLAN.md §1A):** Engram is the **reasoning/decision memory** layer (why/decided/what-bit-us) — *not* a code-structure graph (Graphify/CodeGraph). Those are complementary and can coexist. The validated wedge: an **editable, IDE-embedded graph pane** for reasoning memory + conflict surfacing + local — the closest concept (Cairn) has no UI/traction.

## Status (2026-07-20)
- **Shipped 2026-07-20 (v0.6.1 — the embed-model pin fix; commit e136b5a):** v0.6.0's first field bug, planted by the first real model switch and caught by the next graph write: `ensure_embed_model` stamped the new identity AFTER the re-embed loop, so on the tepin backend the file pinned its vectors under the OLD model name (tepin stamps every vector write with the store's *recorded* model) and all later writes died with `embedder_mismatch`. Fix: stamp before the loop; the matching-identity path now also backfills missing vectors (mid-swap crash heals at next open). Red-green regression test `tepin_embed_model_swap_keeps_writes_working` — LESSON: engine-level guard tests must run on BOTH backends, the sqlite-only twin could never see this. Live store repaired by a round-trip swap under the fixed guard (210/210 re-pinned). Tests 172.
- **Shipped 2026-07-20 (v0.6.0, second wave — TepinDB cutover + model selection + MCP transport; commit d9c2628, tag v0.6.0):** (1) **`Store` trait extracted** (store.rs): ~50 primitives + PROVIDED pure-Rust composites (hybrid fusion, neighbors, traverse, decay_candidates, brief reads) so every backend shares one behavior; `SqliteStore` (store_sqlite.rs) is the reference impl overriding only what SQL answers faster; the two `conn()` leaks (/system, doctor) sealed behind trait `stats()`/`health()`; Engine holds `Box<dyn Store>` (`Engine::new` stayed `impl Store` — zero call-site churn; `with_store` for the dispatcher). (2) **TepinDB driver** (store_tepin.rs, dep = tepindb git rev 1e98ee5 primitives tier, default-features OFF — Engram brings its own embedder): nodes/edges/suspects/audit/meta collections in `.engram/graph.tepin`, manual one-vector-per-node + BM25 over [title,body,tags,code_refs], driver-side sentinel snippets, batch-atomic cascades, zero-padded-seq audit ids + meta counter; GOTCHAS: purposed-but-empty collections error `collection_not_found` on reads (driver maps to empty), and tepin pins one (model_id,dim) per file with NO unpin — `reset_vectors` on tepin = whole-file rebuild behind an RwLock'd Db handle. `open_store` dispatches by `.tepin` extension. (3) **`engram-alpha migrate`**: export/import for the graph (embeddings regenerated), suspects + audit carried VERBATIM (journal replayed oldest-first, import's own row lands last), counts verified, graph.db untouched as backup; **`resolve_db_path`** makes a `graph.tepin` sibling win over `graph.db` at every open — hooks/.mcp.json/registry/IDE wiring survive the cutover untouched. Verified by migrating the real 199-node graph (briefs identical). CAUTION: an MCP stdio process started pre-migration keeps writing to the old sqlite file — /mcp reconnect after migrating; also never copy a live graph.db without its -wal (recent writes live there). (4) **Model selection shipped** (the "Later/maybe" §7A item + Intent resolved): core `cortex.rs` (machine-level `~/.engram/models.json`, presets — bge-small/bge-base/all-MiniLM embeddings, jina-turbo/bge-reranker-base rerankers, nli-deberta — + custom-by-URL specs w/ dim+pooling), `GET/POST /models` (trait `ModelAdmin` in engram-http, CLI's `CortexAdmin` implements — curl provisioning into `~/.cache/engram/<name>/` stays CLI-side), LIVE hot-swap (RwLock'd ModelSet + push into every `hub.engines()`; embedding swaps run `Engine::ensure_embed_model` per engine: reset vectors to new width + full re-embed + `EmbedModelId` stamp; closed projects re-embed lazily at next open; fake-embedding daemons refuse), `Embedder::name()`, System-pane "Choose models" UI (per-role dropdowns, custom URL, threshold-uncalibration warning; also Backend row + redb journal wording). Verified live on BOTH backends: bge-small→all-MiniLM→back with zero restarts. Tests now 163 (core 107 incl. both-backends conformance battery + model-guard tests, http 24 incl. tepin-served API e2e, mcp 20, cli 12); clippy -D warnings + fmt + eslint/stylelint clean. Threshold recalibration for swapped embedding models stays deferred (UI warns). The live cutover first hit redb's exclusive per-process lock (brief/doctor/stdio-MCP vs the daemon — WAL coexistence gone) and was briefly rolled back; the user then directed the unblock, so **the MCP transport migration landed the same day**: the daemon mounts rmcp `StreamableHttpService` at `/mcp` (STATEFUL — one session = one Engram instance over the shared hub, per-session audit attribution preserved; MCP writes now reach the pane's SSE feed), and `engram-alpha mcp` became a thin client — healthy daemon owning the db (daemon.json + /health db match via `daemon_for`) → verbatim stdio↔HTTP passthrough (rmcp Service<RoleServer> forwarding to a Peer<RoleClient>, get_info mirrors the daemon); tepin store with no daemon → spawn `serve --http-only` detached + wait + bridge; else direct open (sqlite unchanged). `brief` uses GET /brief, doctor reads /system when a daemon owns the store; zero wiring files changed (.mcp.json/hooks still launch the same stdio command). e2e in engram-mcp `transport_tests` + live probes. **THIS repo now LIVES on `.engram/graph.tepin`** (208 nodes migrated; graph.db kept as the pre-cutover backup; daemon on 8787 is the sole lock-holder). Tests 164; clippy/fmt clean. REMINDERS: an MCP session started pre-cutover writes to the stale sqlite file — /mcp reconnect after migrating/redeploying; a SECOND daily repo on tepin needs the machine-level hub daemon first (a sibling tepin store is only federable by whichever daemon opens it first). deploy-pane.sh health-checks against graph.tepin when present. **Follow-ups landed same session:** tepin DB-capability test battery (edge indexes vs full-scan agreement at volume, write-read sequences + audit-seq monotonicity across reopens, engine-level interleaved update/archive/delete consistency, 120-node import idempotency, 8-thread serialized-engine hammer — tests now 171); the `*_model_dir` path helpers moved OUT of the `fastembed` feature gate (engram-http now builds standalone); `/models` endpoint tests via a fake ModelAdmin; **SECURITY.md** written (threat model + measures + honest gaps: CORS allowlist and model checksum pinning are pre-release hardening in PLAN §10, at-rest encryption is explicitly post-this-release by user decision). **First wave, same release (multi-project memory, PLAN §7C):** read-federation on SQLite, hub-shaped from day one. **Core:** `registry.rs` (`~/.engram/registry.json`, `ENGRAM_HOME` override, upsert-by-root with stable ids + deduped slugs; `home`/`all` reserved) + `hub.rs` (lazy `EngineFactory` opens, one shared model runtime via `Arc` blanket impls on Embedder/Reranker/Nli, `search_all` with `project` provenance + `CROSS_PROJECT_PRIOR` 0.85, `check_claim_all`, `brief` with a `HOME_BRIEF_RESERVE` home-graph section, `promotion_candidates` for Principles/Cautions recurring across projects — home copy suppresses). **HTTP:** `/projects` (list/register-by-path/DELETE) + every route scoped under `/projects/{sel}/…` via a pre-routing URI rewrite (KEY GOTCHA: `Router::layer` middleware runs after route matching — the rewrite wraps the router through an outer `fallback_service` + `map_request`); per-project SSE channels; writes to `all` are 400 with a home-graph pointer. **MCP:** every tool takes optional `project` (omit=current, name/id=that project incl. writes, `home`, `all`=reads only on search/check_claim), new `list_projects` tool, INSTRUCTIONS teach cross-project capture + "remember this globally"→home. **CLI:** `build_hub` (models loaded once, factory shared), serve/mcp/setup all register the repo; `brief` rides the hub (home section) but never registers. **Pane:** ProjectSwitcher (brand area, add-by-path), registry view in System, Checkup "Find promotion candidates" + promote-to-home (copies, never deletes; code_refs dropped as repo-specific). **Deferred deliberately:** formal `Store` trait (waits for the TepinDB driver — one impl today would be delegation ceremony around a `conn()` leak); skill-variant docs for the `project` param. Tests 154 (core 100, http 23, mcp 20, cli 12 — incl. hub federation e2e, scoped-route e2e, all-write refusals, /fs/dirs); clippy -D warnings + fmt clean. **Follow-ups landed same day and DEPLOYED locally (deploy-pane.sh, daemon on 8787):** (a) folder picker replaced raw add-by-path — browsers can never reveal absolute paths, so `GET /fs/dirs` (daemon-side listing: dirs only, dot-dirs hidden, engram/git badges) feeds a navigator in ProjectSwitcher (↑/~/editable path/Add-this-folder); (b) the brief ends with an "Other project graphs on this machine" roster (Hub::projects_brief_section — only when a factory is present, home listed last, rides the same char budget); (c) all three skill variants + plugin copy gained the "Multi-project memory" section (project etiquette: sibling knowledge into ITS graph, home = user-level, all = reads only, no cross-graph edges; hardlink `.claude/skills/engram` preserved — edit in place, never replace); (d) root README gained "One memory across your projects". **tepindb registered on the live registry** (via POST /projects — MCP deliberately has NO register tool; enrolling paths stays a user/system gesture) and the §7C first milestone Intent is RESOLVED: cross-project add_note landed tepindb's bootstrap Intent (00abbprfqv47 in ITS graph) and search(project:"tepindb") retrieved it, Resolution 00abbq45ur5z answers it. Known deferrable: /fs/dirs rides the same permissive CORS as the whole API — localhost CORS hardening is worth a PLAN line pre-release. **Released as v0.6.0 (2026-07-20): one feat commit d9c2628, draft-release.yml → published, all five artifact jobs green, darwin checksum verified.**

## Status (2026-07-13)
- **Shipped 2026-07-13 (v0.5.1 — digestion; commit 2eba4ce):** PLAN §7B implemented both tiers and verified by a first live digest run (a Claude Opus session, same day). **Tier 1**: `engram_core::digest::scan` (new `digest.rs`) — gitignore-aware walk via the `ignore` crate with `require_git(false)` (a non-git tree still skips node_modules), `log`/`logs`/`tmp`/`temp` dirs dropped, uppercase-only `\b(TODO|FIXME)\b` markers (optional `(author)` tag) → FIXME nominates Problem / TODO nominates Intent, marker text redacted via `redact::scrub` + clipped to 200 chars; binary (NUL in first 8KB), >1MB, or unreadable files are counted skips — the scan is infallible by design; caps reported via `truncated` (500 candidates), never silent. Exposed HTTP-only as `POST /digest/scan` (root derived from DB path like `/drift`, runs via spawn_blocking off the engine lock; deliberately NO MCP tool, NO CLI command). **Tier 2**: the variant-less `engram-digest` skill (canonical `skills/engram/digest/SKILL.md`) — explicit invocation only ("digest this project", `/engram:digest`), current-working-tree target with git history as supporting why, 8 copy-paste-safe worked examples (one per node type, each teaching one feature: because-triple, answers+close, builds-on, replaces, about+code_refs, durability, tags, open status), every write carries `session_id: digest-<YYYY-MM-DD>` (reviewable batch; session quarantine remains a separate §10 item) and verdict handling is mandatory (no bulk bypass). Distribution mirrors the capture skill: plugin ships a verbatim copy + `commands/digest.md` (sync test `plugin_digest_skill_matches_canonical`), `engram-alpha setup --cli claude` installs it to `.claude/skills/engram-digest` (same symlink guard), this repo dogfoods via symlink. Tests now 146 (core 96, http 21, mcp 17, cli 12).
- **Shipped 2026-07-13 (v0.5.0 — the local-cortex release; commit 23943f8, tag v0.5.0):** (1) **Reranker precision layer shipped**: `Reranker` trait + `FastReranker` (jina-reranker-v1-turbo-en, 38M, fastembed TextRerank, shared model cache) as an optional Engine component — `search` over-fetches 3× (clamp 12..50), cross-encodes title+snippet against the query, score = sigmoid(logit) × (1 + RERANK_TRUST_WEIGHT × trust), truncates, and only THEN attaches neighbors + stamps last_seen (discarded candidates are never "seen"); loaded only by serve/mcp with real embeddings (`build_engine(..., rerank)`), load failure degrades to hybrid order with a warning; loading is local-dir-first like the embedder (`ENGRAM_RERANKER_DIR`, else `~/.cache/engram/jina-reranker-v1-turbo-en` with model.onnx + 4 tokenizer files) because the hf_hub fallback proved flaky in practice (first deploy failed with "Failed to retrieve model file" — the files were then fetched manually via curl from huggingface.co/jinaai/jina-reranker-v1-turbo-en); `/system` reports `reranker`, System pane shows a "Local models" section. (2) **Phase-A NLI eval harness**: `scripts/nli-eval.py` (dev tool, python+transformers, never a runtime dep) turns judged suspects into a labeled NLI corpus (conflict≈contradiction, replaces≈entailment, dismiss≈neutral; verdict recovered from the created edge) and scores candidate models on it — dogfood corpus currently 19 pairs, needs more judged conflicts before the finecat-nli-m vs ModernBERT-base-nli bake-off is meaningful. (3) **PLAN §7A "local cortex"** written: four layers (recall/precision/logic/decomposition), the **models-don't-validate** Principle (third after time/exposure: model verdicts NOMINATE — queue order, suggested verdicts, merge/split candidates, edge verbs — only judgments move trust), audit-panel buttons, offline extractive split/merge design, deferred organs (GLiNER, SetFit worthiness gate), NLI model decision (finecat-nli-m). (4) **Pre-rename `engram` binary support DROPPED** (v0.4.x shim sunset as designed): release.yml legacy-asset blocks deleted, brief-hook `command -v engram` fallback removed (both copies, sync-tested), VSCode legacy binary detection removed; doctor warnings + setup repair + installer stale-binary cleanup KEPT as migration-out assistance. (5) **NLI logic layer shipped** (user redirect mid-build: simplest max-compatibility model): runtime = `Xenova/nli-deberta-v3-small` quantized ONNX (~35MB, downloaded by the CLI via curl direct from HF into ~/.cache/engram/nli-deberta-v3-small, ENGRAM_NLI_DIR override; finecat-nli-m has NO onnx export and stays the eval-side pick) — `nli.rs`: Nli trait + FakeNli + FastNli (ort+tokenizers pinned to fastembed 5.17.2's versions, id2label read from config.json); suspects carry nli_label/nli_score hint columns (migration), brief sorts contradiction-hinted suspects first and prints the hint; `check_claim` (engine + POST /claims/check + MCP tool): supports/contradicts/silent buckets. (6) **Checkup pane panel** (topbar, next to Review): Find hidden conflicts / Find duplicates / Check open problems (POST /audit/{conflicts,duplicates,answered}) + free-text claim check + instant structural checks (Decisions without a because, orphans); every button emits nominations into Review, nothing self-applies. (7) **Co-reference finding (dogfooded live)**: MNLI-class models presuppose both sentences describe the same situation — a 0.70-floor sweep queued 178 junk "contradictions" (0.99–1.00 confident) between unrelated shipped-feature titles while canonical MNLI pairs scored perfectly; conflict sweep floor kept at CONFLICT_SUSPECT_SIMILARITY + NLI_SWEEP_MIN_CONFIDENCE 0.8 gate; the floor itself was then raised 0.85→0.88 (2026-07-13, dogfood retune, verified live) after the judged-suspects table showed a 96% false-positive rate (26/27 dismissed, all ≤0.883; the one confirmed conflict at 0.916) — same-genre vocabulary inflates bge-small cosine into the 0.85–0.87 shelf; sub-threshold ambition still waits for a domain-calibrated model (judged-suspects corpus). Manual model sanity test: `cargo test -p engram-core -F fastembed real_nli -- --ignored`. (8) **Same-turn write verdict completed**: add_note/update_node responses now also carry `missing_code_refs` + refs_note (write-time drift check — engine.set_repo_root derived from the DB path, set by serve/mcp; no root = check skipped, never guessed) and the dupe-guard `matched` response carries an NLI hint — `nli_label=contradiction` on a ≥0.90 match means a NEGATED near-duplicate ("use X" vs "don't use X", the one place co-reference holds and the one case where blind merging corrupts canon; action_required tells the assistant to read before merging and prefer conflicts-with). update_node_checked returns a named `CheckedUpdate` struct. (9) **/system `models` array**: name/role/path/active per cortex model (embeddings honesty via engine.embeddings_are_fake); System pane renders uniform rows with on-disk paths. (Reminder for future MCP-side work: after a rebuild the stdio MCP session keeps running the OLD binary — /mcp reconnect before testing MCP features.)
- **Shipped 2026-07-12 (v0.4.2 — trust v2; stayed on 0.4.* so the v0.4-gated legacy transition assets keep shipping):** the trust model rebuilt around two principles from the first external feedback (a redditor: time-decay buries rare true constraints; a sandbox tester: retrieval stamping last_seen lets exposure certify wrong notes — "don't teach retrieval to certify its own outputs"). *Time doesn't validate:* stable durability holds trust flat until a live `conflicts-with` stamps `demoted_at` on the older endpoint (add_edge AND update_edge retype; pins are skipped), then ramps — withdrawing the evidence (edge resolved/dismissed/retyped/deleted) clears the demotion (`undemoted` audit rows); drift is review-only, it deliberately never demotes (wrong-cwd scans must not mass-bury the graph); episodic 183d, volatile 30d (new constant); open Problems/Intents never fade. *Exposure doesn't validate:* new `confirmed_at` column is the unapproved trust anchor (stamped by update_node — which also clears demotion — and approve); `last_seen` is observability-only (search/brief still stamp it; nothing reads it for trust); brief canon ordering switched to COALESCE(approved_at, confirmed_at, created_at) to break its self-reinforcing inclusion loop. **Pin** = `trust_override` REAL (user-only: POST /nodes/{id}/pin {value|null}, no MCP tool — like hard delete): constant trust, exempt from decay/auto-archive/demotion, PINNED marker in the brief, 📌 badges + `pinned` trust filter in the pane, `pinned` filter on list_nodes. Approve ladder: created 50% → confirmed 60% → approved 100% (re-approve restamps; DELETE /nodes/{id}/approve revokes approval + pin). Ranking got a small type prior in trust_boost (Principle/Caution +0.05, Decision/Insight +0.04) — type never touches trust itself. NodeDetail footer reordered Approve · Edit · Pin · […] · Delete ([…]: Confirm still true / Revoke approval / Set constant trust %); every trust element has a tooltip and each card renders a plain-words trust explanation (`frontend/src/constants/trust.ts` mirrors policy.rs — keep them in sync). Migration backfills confirmed_at = last_seen once and freshly anchors volatile nodes (their window shrank 183d→30d — an old anchor would back-date them straight past the decay TTL); import applies the same confirmed_at backfill so a pre-v2 JSON backup restores healthy; a Claude-sourced replaces verdict on a pinned older node errors (Error::Pinned, HTTP 409) — user verdicts proceed. New audit actions: unapproved | pinned | unpinned | demoted | undemoted. The sandbox tester's adversarial scenario is a regression test (`exposure_does_not_preserve_the_false_note`). Skill trust sections rewritten in all variants (+ plugin copy synced; `.claude/skills/engram/SKILL.md` is a **hardlink** to the aggressive variant).
- **Shipped 2026-07-12 (v0.4.1):** pane fixes (settings menu scrolls with a viewport cap + contained overscroll; topbar z-index 10→12 so its dropdowns — settings/filter/search/burger — escape their stacking context and layer above open drawers); **write-verdict protocol** (write responses now also carry `suspects` — the look-alike pairs the write just queued, with an inline `action_required` instruction; `update_node_checked` returns them too; skill overhaul across all variants + agents blocks: decisions are captured unprompted since feature requests usually hide one, every write response is a verdict — matched → merge, warnings → check canon, suspects → judge NOW via resolve_suspect — and a genuine contradiction is the one audible exception to silent capture); **Subagents section** in the skills (subagents share the MCP connection and can search/brief but start cold and write under the parent session id — recall flows down via prompts, capture flows up via findings); **Windows release zip dropped** (release.yml ships only the raw .exe — install.ps1, self-update, and manual downloads all use it directly; nothing consumed the zip).
- **Phase 0 AND Phase 1 complete & verified** (146 tests: core 96, http 21, mcp 17, cli 12). `engram-alpha serve` serves the HTTP API + SSE + the embedded Vue pane at `http://127.0.0.1:8787`; release binary at `~/.cargo/bin/engram-alpha`. Latest published release: **v0.6.1** (2026-07-20 — v0.6.0 plus the embed-model pin fix). Release flow: push → `gh workflow run draft-release.yml -f version=X.Y.Z` → publish the draft (gh release edit --draft=false or the GitHub UI), which tags and builds artifacts. All crates share `[workspace.package] version` (0.6.1) which release.yml stamps from the tag — never version a crate individually; `claude-plugin/.claude-plugin/plugin.json` must carry the same version (a cli test enforces it — the plugin installs from the repo, not from release artifacts).
- **Shipped 2026-07-11 (v0.4.0):** **binary renamed `engram` → `engram-alpha`** (crate names, MCP server key `engram`, `.engram/`, `/engram:` plugin namespace deliberately unchanged; IDE plugins + brief hook fall back to a pre-rename `engram` binary; release assets now `engram-alpha-<tag>-<target>.*`); **`engram-alpha doctor`** (`crates/engram-cli/src/doctor.rs`: WAL/quick_check/count integrity, model cache, daemon-vs-repo DB match over `/health` via raw HTTP/1.0, `.mcp.json`/hook/gitignore/codex wiring checks; non-zero exit on failures); **Codex desktop app coverage** (shares `~/.codex/config.toml` + AGENTS.md with the CLI; app-install detection in `detect_agents`; setup/doctor warn about the app's unreliable launch cwd — pin `cwd`/absolute `--db`; the app ignores project-local `.codex/config.toml`, openai/codex#13025); **timeline pane view** (History section in NodeDetail: `replaces` chain oldest-first, dot timeline, current marked, retirement notes, click-to-jump; fetch gated on the node carrying a `replaces` edge); **JetBrains plugin zip now bundles its README** (buildPlugin task in `engram-jetbrains/build.gradle.kts`); **bulk MCP tools** (`list_nodes` full-fidelity paged read with filters — the lossless export/review read; `update_nodes`/`add_notes` batch writes, ≤100 items, per-item results, same dupe checks/warnings as the singular forms — born from a graph-curation session that took ~20 sequential update_node calls); **full-field retrieval index** (embeddings + FTS now cover tags and code_refs — embed composition v2 stamped in PRAGMA user_version, guarded one-time re-embed on the first real-embedder open, NEVER under `--fake-embeddings`; FTS rebuilt with a code_refs column; a "policy.rs"-style query reaches every node citing the file); **search-match highlighting** (latent mismatch: the pane always expected <mark> but the store emitted literal brackets — store now marks matches with private-use sentinels U+E000/E001, MCP rewrites them to [ ] for assistants, SearchBar renders them as real <mark> highlights); **v0.3→v0.4 migration path** (release.yml additionally ships legacy-named `engram-*` assets containing the new binary under the old filename, gated to `v0.4.*` tags so the shim self-sunsets at v0.5 with zero maintenance — old binaries' `update` lands them on current in place, and once updated they fetch `engram-alpha-*` assets forever; installers remove a stale pre-rename binary only after verifying it's OURS via the stable `--help` marker "Durable graph memory" — the name is contested; `setup` repairs wiring that launches a pre-rename binary across .mcp.json/gemini/opencode/kilo JSON shapes + the codex TOML, and doctor warns on any it finds); all READMEs + PLAN refreshed for the rename; **System info pane view** (Settings → System: GET /system serves doctor's daemon-side facts as JSON — binary version, uptime/pid/repo root, store health incl. embed composition, model cache, per-assistant wiring with pre-rename flags; harness probes moved from engram-cli to `engram_core::harness` so setup/doctor/daemon share one implementation, CLI re-exports keep the old paths).
- **Shipped 2026-07-10 (v0.3.0):** **Claude Code plugin** (repo-root `.claude-plugin/marketplace.json` + `claude-plugin/` with relaxed-skill/hook copies, `/engram:setup` + `/engram:pane` commands, **no global MCP config** — deliberate, see PLAN §10; per-repo wiring via new `engram-alpha setup --mcp-only`; verbatim-copy sync tests in `crates/engram-cli/src/setup.rs`; the hook defers when `ENGRAM_HOOK_SOURCE=plugin` and the repo registers its own — never brief twice); pane CRUD + tags end to end; audit journal (per-mutation rows with binary context, pane Audit log drawer, `audit` MCP tool; node mutations attribute the *acting* session, creator only on "created" rows); session-start brief hook (`hooks/session-brief.sh` + `engram-alpha setup`); verified code refs / drift (engine scan + `GET /drift` + `list_drift` MCP tool + Review-panel section + drifted badges + health-strip count); `timeline` (MCP tool + `GET /nodes/{id}/timeline`, replaces-chain oldest-first, topology-ordered); `engram://node/{id}` MCP resource (list = 25 newest, template for the rest); brief format v2 (16k budget, node id on every record, Recently-added right after suspects, Decisions capped 7 with 80-char excerpts); SidePanel drawer shell (one drawer per side, drag-resizable session widths min…50vw, macOS overscroll contained inside the frame); four canvas layouts (Skyline default / Nebula / Archipelago / Orbit — see the graph's layout Decision for the physics); skill overhaul (value story, maintenance duties incl. close-the-loop + drift repair, example flows, unhappy-path protocol — all variants + agents/*.md).
- **Phase 1 (v0.2.0):** conflict scan (local embedding candidates ≥ 0.85 → `suspects` queue; write-time + 6-hourly daemon sweep + pane "Scan now"; judgment via pane worklist or Claude's `list_suspects`/`resolve_suspect` — verdicts: conflict / replaces (archives the older) / dismiss); decay pass (`POST /decay`, 14-day TTL past stale-crossing, Claude-authored unapproved episodic/volatile only); brief lists top-8 suspects; graph-health strip; NodeDetail edit mode incl. type reclassification; node provenance section; Memory Lens (brief viewer) in settings; stdio MCP stamps a per-process fallback `session_id` (`mcp-…`).
- **Name (locked):** the product is **"Engram Alpha"** (Alpha = part of the name, not a version); the binary is `engram-alpha` (since v0.4.0), repo/JetBrains plugin stay `engram`, VSCode extension publishes as `engram-alpha`. See PLAN §0/§1A for the four-way "engram" collision.
- **JetBrains plugin** (`engram-jetbrains/`, package `dev.techtheist.engram` — don't rename): JCEF tool window (right anchor) + editor-tab mode; `./gradlew buildPlugin` → zip; user-tested in-IDE.
- **VSCode extension** (`engram-vscode/`): Webview pane in the **secondary sidebar (right)** + `.mcp.json` configurator + daemon status bar; `vsce package` → vsix; user-tested in-IDE.
- **Skill:** three capture variants in `skills/engram/` (**aggressive / normal / relaxed**; relaxed is the recommended default for users, **aggressive is what this repo dogfoods** via `.claude/skills/engram`).
- **Known divergence:** MCP currently runs as a stdio `engram-alpha mcp` process (wired via `.mcp.json`), not the daemon-hosted streamable HTTP from PLAN §0 — deliberate deferral; the user owns that migration. Don't rewrite it unprompted.
- **Pane rebuild cycle:** run **`scripts/deploy-pane.sh`** (add `--vsix` / `--jetbrains` to also rebuild plugins; real embeddings are the default — `--fake` only for throwaway DBs, since MCP writes real vectors and a fake-embedding daemon searching them is noise). It builds the pane, reinstalls the binary, restarts the daemon on the repo's absolute DB path, and verifies `/health` serves the right DB — never hand-chain `cd`/build/restart (relative `--db` from the wrong cwd silently creates an empty graph).
- **Next (PLAN §10 near-term):** bundle skill + `.mcp.json` bootstrap into the IDE plugins; `runIde`/`verifyPlugin`; MCP transport migration; session quarantine (journal substrate shipped); file-read match hook (design TBD — needs a code_refs-match endpoint + noise control). Later: Cursor/Windsurf MCP wiring; app-level DB encryption (far down). **Out of scope permanently:** multi-user & repo sync — a future enterprise product, not Engram Alpha (user decision 2026-07-10).

## Locked decisions (don't relitigate without reason)
- **Open-source, MIT.** Goal: portfolio/credibility → optimize for docs & DX.
- **Backend: Rust** (`rmcp`, `rusqlite` bundled, `sqlite-vec`, `fastembed`). No Node.js runtime dependency.
- **Frontend:** Vue 3.5 + TS + Vite + **Bun**, Pinia, vue-router, Tailwind 4, **Vue Flow** (`@vue-flow/*`) for the graph, vueuse, markdown-it + dompurify, ESLint (flat config) + Stylelint — `bun run lint` / `lint:style` in `frontend/`.
- **Scope: per-repo graph only** (no cross-project layer in v1).
- **Embeddings: local-only** (fastembed/ONNX). No remote, no keys.
- **Build first: browser standalone** (also serves Claude Desktop). IDE wrappers later.
- **Capture: cooperative (MCP) first** — Claude writes via MCP tools + a shipped skill. Ambient hooks later.
- **Conflicts: passive in v1**, surfaced via RAG retrieval. Mid-session push (MCP channels) is a planned Phase-2 feature.
- **Storage: `.engram/graph.db` in repo, git-ignored** (personal). Share later via JSON export, not the binary DB.
- **Backend run (v1): `engram-alpha serve`** — one local daemon (HTTP + MCP + **SSE**).
- **Deletion:** auto-supersede; **hard-delete is user-only** (cascades edges). **Bootstrap:** empty start. **Export:** JSON in v1. **Secrets:** skill rule + backend redaction pass. **Episodic growth:** stale provisional nodes decay/archive.
- **Merge:** search-before-write is **recommended once the graph is big enough that duplicates are likely** — not a hard gate on small graphs. `add_note` always runs a same-type similarity pre-check and returns `{matched, created: false}` on a near-dupe (then `update_node` the match). **Concurrency:** SQLite **WAL**, serialized writes. **Daemon:** **one `engram-alpha serve` per repo** (HTTP + MCP + SSE). **Pane (v1):** render full graph; scaling deferred.
- **Approach:** **dogfood on Aggressive mode** (we're the first user); **minimalist** code — clear names + deps' public docs over inline comments.

## Ontology conventions (see PLAN.md §4)
- **8 node types only:** Principle, Decision, Caution, Problem, Resolution, Insight, Intent, Anchor. Do **not** add new types.
- **7 edge types, sentence-shaped:** `about`, `because`, `answers`, `builds-on`, `replaces`, `conflicts-with`, `needs`. A triple must read as English (e.g. *Decision because Principle*).
- **No generic `relates_to`.** If you can't pick a real verb, don't link.
- **Durability** (`stable` / `episodic` / `volatile`) governs staleness. Never store volatile implementation details unless asked.
- High-value edges are `replaces` and `conflicts-with` — they make the graph *active*.

## Where things go
- `crates/engram-core` — graph store, RAG, librarian, domain types.
- `crates/engram-mcp` — rmcp stdio server. `crates/engram-http` — axum API + embedded pane (rust-embed). `crates/engram-cli` — the `engram-alpha` binary (serve / mcp / doctor / setup).
- `frontend/` — Vue app (Bun). `engram-jetbrains/` — JetBrains plugin (JCEF). `engram-vscode/` — VSCode extension (Webview).
- `skills/engram/{aggressive,normal,relaxed}/SKILL.md` — the cooperative-capture skill variants (relaxed = user default, aggressive = dogfood).

## Resolved behaviors (v1 — see PLAN.md §6A)
- **Capture:** Claude judges what's worth saving (no objective gate); Claude is the sole gatekeeper via the skill (no separate librarian pass yet). Duplicates → **merge into existing** + bump confidence. Claude nodes start **provisional**, earn trust by reconfirmation/approval, stale ones decay.
- **Anchors:** free-text responsibility label (+ optional file refs); **flexible granularity** (Claude decides); auto-created/reused; on refactor, **fuzzy re-match and flag** if unsure.
- **Retrieval:** session-start (small, ~5–8, token-capped) **+ on-demand**; returns **matches + 1-hop subgraph** (conflict/replace neighbors prioritized); **hybrid ranking + trust boost**.
- **Skill writes:** **batch at stopping points**, **fully silent** (transparency is the pane, not chat), **both** auto- and manually-invoked (`/engram`), default mode **Relaxed**.

## Retrieval & write surface (implemented 2026-07-02)
`brief` (session-start digest, MCP tool + `GET /brief` + `engram-alpha brief`); search hits carry ≤5 1-hop neighbors (conflicts/replaces first) and use relevance×trust scoring (FTS5 OR-recall + cosine); `add_note` self-checks same-type dupes (`{matched, created:false}`); writes return `warnings` near conflicted/superseded nodes; `unlink`/`update_edge` let Claude repair mislinks (node hard-delete stays user-only).

## Open questions (implementation-level — PLAN.md §11)
Decay/promotion numbers (implemented, untested); fuzzy re-match algorithm; neighbor-cap/brief-quota tuning; pane review UX (matters because writes are silent). Future: tags + pane filtering (PLAN §10 Later).
